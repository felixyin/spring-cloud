server:
  port: 3344

spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: http://qtrj.i234.me:30000/nonsense/spring-cloud.git
          search-paths:
            - config-at-gitrepo # 仓库中的哪个目录作为配置目录
          username: felixyin
          password: Ybkk1027
          skip-ssl-validation: true
      label: master
    refresh:
      enabled: true # 自动刷新配置，必须开启此选项
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest

eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka

# 暴露bus刷新配置的端点
management:
  endpoints:
    web:
      exposure:
#        include: "bus-refresh"
        include: "*"
